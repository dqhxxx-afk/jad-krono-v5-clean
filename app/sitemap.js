import watches from "../data/watches.json";export default function sitemap(){const base="https://jadkrono.com";const routes=["","/collection","/sell-your-watch","/services","/about","/contact","/faq","/privacy-policy","/terms-conditions"];return [...routes.map(route=>({url:`${base}${route}`,lastModified:new Date(),changeFrequency:route===""?"weekly":"monthly",priority:route===""?1:0.7})),...watches.map(w=>({url:`${base}/collection/${w.id}`,lastModified:new Date(),changeFrequency:"weekly",priority:0.8}))];}
